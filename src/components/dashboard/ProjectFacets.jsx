import React from 'react';
import { Card, CardContent, Typography } from '@mui/material';

const ProjectFacets = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">Project Facets</Typography>
        {/* Facets content will be added here */}
      </CardContent>
    </Card>
  );
};

export default ProjectFacets; 