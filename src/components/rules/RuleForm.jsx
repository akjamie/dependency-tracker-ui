import React from 'react';
import { Card, CardContent, Typography } from '@mui/material';

const RuleForm = () => {
  return (
    <Card sx={{ mb: 2 }}>
      <CardContent>
        <Typography variant="h6">Create/Edit Rule</Typography>
        {/* Form content will be added here */}
      </CardContent>
    </Card>
  );
};

export default RuleForm; 