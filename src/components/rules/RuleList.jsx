import React from 'react';
import { Card, CardContent, Typography } from '@mui/material';

const RuleList = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">Rules List</Typography>
        {/* Rules list content will be added here */}
      </CardContent>
    </Card>
  );
};

export default RuleList; 